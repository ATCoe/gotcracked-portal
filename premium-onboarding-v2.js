(()=>{
  'use strict';
  if(window.GotCrackedPremiumOnboarding)return;
  const client=window.supabaseClient;if(!client)return;
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const profile=()=>window.GotCrackedRuntimeProfile||window.GotCrackedOperationsV1?.state?.profile||null;
  const roleLabel=v=>String(v||'staff').replaceAll('_',' ').replace(/\b\w/g,c=>c.toUpperCase());
  let pkg=null,overview=null;

  function installStyle(){
    if(document.getElementById('gc-premium-onboarding-style'))return;
    const s=document.createElement('style');s.id='gc-premium-onboarding-style';s.textContent=`
      .gc-onboarding-command{margin-top:16px}.gc-onboarding-metrics{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin:14px 0}.gc-onboarding-metric{padding:14px;border:1px solid rgba(123,151,181,.18);border-radius:14px;background:rgba(119,151,181,.05)}.gc-onboarding-metric small{display:block;opacity:.68}.gc-onboarding-metric strong{display:block;font-size:22px;margin-top:4px}.gc-onboarding-list{display:grid;gap:8px}.gc-onboarding-row{display:grid;grid-template-columns:minmax(150px,1.2fr) minmax(120px,.8fr) minmax(150px,1fr) auto;gap:12px;align-items:center;padding:11px 12px;border:1px solid rgba(123,151,181,.16);border-radius:12px}.gc-onboarding-row small{display:block;opacity:.7}.gc-onboarding-status{display:inline-flex;width:max-content;padding:4px 8px;border-radius:999px;background:rgba(42,142,247,.11);font-size:10px;font-weight:850;text-transform:uppercase;letter-spacing:.05em}.gc-onboarding-status.complete{background:rgba(35,151,103,.12)}.gc-onboarding-status.pending{background:rgba(225,157,55,.12)}.gc-premium-form-note{padding:11px 12px;border-radius:12px;background:rgba(42,142,247,.07);font-size:12px;line-height:1.5}
      .gc-welcome-dialog{width:min(720px,calc(100vw - 28px));border:1px solid rgba(77,163,255,.22);border-radius:24px;padding:0;background:var(--surface,#fff);color:var(--text,#122033);box-shadow:0 30px 90px rgba(0,0,0,.36)}.gc-welcome-dialog::backdrop{background:rgba(4,12,24,.76);backdrop-filter:blur(7px)}.gc-welcome-shell{display:grid;grid-template-columns:190px 1fr;min-height:480px}.gc-welcome-rail{padding:26px 18px;background:linear-gradient(160deg,rgba(42,142,247,.14),rgba(42,142,247,.03));border-right:1px solid rgba(123,151,181,.15)}.gc-welcome-mark{font-size:11px;font-weight:900;letter-spacing:.12em;text-transform:uppercase}.gc-welcome-steps{display:grid;gap:12px;margin-top:28px}.gc-welcome-step{display:flex;gap:9px;align-items:center;font-size:12px;opacity:.52}.gc-welcome-step i{display:grid;place-items:center;width:24px;height:24px;border-radius:50%;background:rgba(123,151,181,.13);font-style:normal;font-weight:900}.gc-welcome-step.active,.gc-welcome-step.done{opacity:1}.gc-welcome-step.active i{background:#2a8ef7;color:#fff}.gc-welcome-step.done i{background:rgba(35,151,103,.15);color:#168254}.gc-welcome-main{padding:30px;display:flex;flex-direction:column}.gc-welcome-main h1{margin:4px 0 8px;font-size:28px}.gc-welcome-copy{font-size:14px;line-height:1.65;max-width:56ch}.gc-welcome-main form{display:grid;gap:12px;margin-top:16px}.gc-welcome-main label{display:grid;gap:6px}.gc-welcome-main input,.gc-welcome-main textarea{width:100%;color:inherit}.gc-welcome-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.gc-welcome-callout{padding:14px;border-radius:14px;background:rgba(42,142,247,.08);margin:15px 0}.gc-welcome-actions{display:flex;gap:9px;align-items:center;margin-top:auto;padding-top:22px}.gc-welcome-checks{display:grid;gap:10px;margin:16px 0}.gc-welcome-checks label{display:flex;gap:9px;align-items:flex-start;padding:11px;border:1px solid rgba(123,151,181,.16);border-radius:12px}.gc-welcome-checks input{width:auto;margin-top:3px}.gc-welcome-message{min-height:20px;font-size:12px}.gc-welcome-message.error{color:#d74747}.gc-welcome-complete{text-align:center;margin:auto;max-width:430px}.gc-ready-icon{width:72px;height:72px;border-radius:50%;display:grid;place-items:center;margin:0 auto 18px;background:rgba(35,151,103,.12);font-size:30px}html[data-theme="dark"] .gc-welcome-dialog{background:#101c2a;color:#e9f2fb}
      @media(max-width:720px){.gc-onboarding-metrics{grid-template-columns:1fr}.gc-onboarding-row{grid-template-columns:1fr auto}.gc-onboarding-row>[data-secondary]{grid-column:1/-1}.gc-welcome-shell{grid-template-columns:1fr}.gc-welcome-rail{border-right:0;border-bottom:1px solid rgba(123,151,181,.15);padding:16px 18px}.gc-welcome-steps{grid-template-columns:repeat(4,1fr);margin-top:12px;gap:6px}.gc-welcome-step{font-size:0}.gc-welcome-step i{font-size:11px}.gc-welcome-main{padding:22px}.gc-welcome-grid{grid-template-columns:1fr}}
    `;document.head.appendChild(s);
  }
  async function rpc(name,args={}){const{data,error}=await client.rpc(name,args);if(error)throw error;return data;}

  function upgradeManagementForm(){
    const form=document.getElementById('staff-invite-form');if(!form||form.dataset.gcPremium==='true')return;
    form.dataset.gcPremium='true';const card=form.closest('.card');
    const title=card?.querySelector('h2');if(title)title.textContent='Welcome a new employee';
    const intro=card?.querySelector('p.subtle');if(intro)intro.textContent='Create one secure hire package: Discord identity, private Portal invite, first-day details, and a guided setup checklist.';
    const recovery=form.querySelector('input[name="recoveryEmail"]')?.closest('label');if(recovery){recovery.firstChild.textContent='Pre-employment email (optional)';let small=recovery.querySelector('small');if(!small){small=document.createElement('small');recovery.appendChild(small);}small.textContent='For pre-hire and welcome communication only. It is not a Portal password login.';}
    const role=form.querySelector('select[name="role"]')?.closest('label');
    if(role&&!form.elements.jobTitle){const grid=document.createElement('div');grid.className='gc-welcome-grid';grid.innerHTML='<label>Job title<input name="jobTitle" maxlength="120" placeholder="Repair Technician"></label><label>First day<input name="firstDay" type="date"></label>';role.insertAdjacentElement('beforebegin',grid);}
    if(role&&!form.elements.welcomeMessage){const welcome=document.createElement('label');welcome.innerHTML='Welcome note<textarea name="welcomeMessage" maxlength="1200" rows="3" placeholder="Welcome to GotCracked. We’re excited to have you on the team."></textarea>';role.insertAdjacentElement('afterend',welcome);}
    const button=form.querySelector('button[type="submit"]');if(button)button.textContent='Create secure welcome package';
    if(button&&!form.querySelector('.gc-premium-form-note')){const note=document.createElement('div');note.className='gc-premium-form-note';note.textContent='No temporary password is created. The employee joins staff Discord, opens the one-time Portal invitation, authenticates with Discord, creates a personal workstation PIN, and completes the guided first-day setup.';button.insertAdjacentElement('beforebegin',note);}
    const second=card?.parentElement?.querySelectorAll('.card')?.[1];if(second){const h=second.querySelector('h2');if(h)h.textContent='Premium onboarding';const list=second.querySelector('.onboarding-steps');if(list)list.innerHTML='<li>Send the polished welcome email through GotCracked pre-employment Gmail.</li><li>The employee joins the private staff Discord and opens the one-time Portal invitation.</li><li>Discord becomes their human Portal identity — no employee password to share or lose.</li><li>Portal guides profile setup, personal PIN creation, workflow expectations, and first-day readiness.</li><li>Management sees progress and can cancel unused invitations.</li>';const note=second.querySelector('.demo-note');if(note)note.textContent='The welcome package includes copy-ready email text, a one-use Discord invite, and a private Portal invitation. Raw invitation tokens are never shown again after creation.';}
  }

  function statusClass(v){return ['completed','active'].includes(v)?'complete':['pending','invite_created','not_started'].includes(v)?'pending':'';}
  async function loadOverview(){const p=profile();if(!p||p.account_type==='shared_workstation'||!['owner','manager'].includes(p.role))return;try{overview=await rpc('get_staff_onboarding_overview');renderOverview();}catch(e){console.warn('Onboarding overview unavailable:',e?.message);}}
  function renderOverview(){
    const staff=document.getElementById('staff');if(!staff||!overview)return;
    let host=document.getElementById('gc-onboarding-command-center');if(!host){host=document.createElement('section');host.id='gc-onboarding-command-center';host.className='card gc-onboarding-command';const two=staff.querySelector('.two-col');two?two.insertAdjacentElement('afterend',host):staff.appendChild(host);}
    const employees=overview.employees||[],invites=(overview.invitations||[]).filter(i=>i.status!=='accepted');const inProgress=employees.filter(x=>!x.onboarding_complete).length,pending=invites.filter(x=>x.status==='pending').length,ready=employees.filter(x=>x.onboarding_complete).length;
    const employeeRows=employees.filter(x=>!x.onboarding_complete).map(e=>{const done=[e.welcome_ack_at,e.profile_confirmed_at,e.security_ack_at,e.workflow_ack_at].filter(Boolean).length;return`<div class="gc-onboarding-row"><div><strong>${esc(e.display_name)}</strong><small>${esc(e.job_title||roleLabel(e.role))}</small></div><span class="gc-onboarding-status">${done}/4 setup</span><div data-secondary><small>Discord ${e.discord_linked?'linked':'pending'} · PIN ${e.pin_ready?'ready':'needed'}</small><small>${esc(String(e.progress_status||'in progress').replaceAll('_',' '))}</small></div><span></span></div>`;}).join('');
    const inviteRows=invites.slice(0,8).map(i=>`<div class="gc-onboarding-row"><div><strong>${esc(i.display_name)}</strong><small>${esc(i.job_title||roleLabel(i.role))}</small></div><span class="gc-onboarding-status ${statusClass(i.status)}">${esc(i.status)}</span><div data-secondary><small>${i.first_day?`First day ${esc(i.first_day)} · `:''}@${esc(i.discord_username||'Discord pending')}</small><small>Expires ${esc(new Date(i.expires_at).toLocaleDateString())}</small></div>${i.status==='pending'?`<button type="button" class="text-button" data-cancel-staff-invite="${esc(i.id)}">Cancel invite</button>`:'<span></span>'}</div>`).join('');
    host.innerHTML=`<div class="card-title"><div><p class="eyebrow">People operations</p><h2>Onboarding command center</h2><p>Invitations, identity setup, and first-day readiness in one place.</p></div></div><div class="gc-onboarding-metrics"><div class="gc-onboarding-metric"><small>Pending invitations</small><strong>${pending}</strong></div><div class="gc-onboarding-metric"><small>In setup</small><strong>${inProgress}</strong></div><div class="gc-onboarding-metric"><small>Ready employees</small><strong>${ready}</strong></div></div><div class="gc-onboarding-list">${employeeRows}${inviteRows||(!employeeRows?'<p class="empty-state">No onboarding tasks need management attention.</p>':'')}</div>`;
  }

  function dialog(){let d=document.getElementById('gc-welcome-dialog');if(d)return d;d=document.createElement('dialog');d.id='gc-welcome-dialog';d.className='gc-welcome-dialog';d.innerHTML='<div class="gc-welcome-shell"><aside class="gc-welcome-rail"><div class="gc-welcome-mark">GotCracked Welcome</div><div class="gc-welcome-steps" data-welcome-steps></div></aside><main class="gc-welcome-main" data-welcome-main></main></div>';document.body.appendChild(d);return d;}
  function activeStep(){const p=pkg?.progress||{};if(!p.welcome_ack_at)return 0;if(!p.profile_confirmed_at)return 1;if(!p.security_ack_at||!pkg?.pin_ready)return 2;if(!p.workflow_ack_at)return 3;return 4;}
  function stepRail(active){return['Welcome','Your profile','Security','Quick start'].map((n,i)=>`<div class="gc-welcome-step ${i<active?'done':i===active?'active':''}"><i>${i<active?'✓':i+1}</i><span>${n}</span></div>`).join('');}
  function message(text='',error=false){const n=dialog().querySelector('[data-welcome-message]');if(n){n.textContent=text;n.className=`gc-welcome-message${error?' error':''}`;}}
  function renderWizard(){
    if(!pkg?.required)return;const d=dialog(),main=d.querySelector('[data-welcome-main]'),step=activeStep(),p=pkg.profile||{},w=pkg.progress?.welcome_payload||{};d.querySelector('[data-welcome-steps]').innerHTML=stepRail(Math.min(step,3));
    if(step===0)main.innerHTML=`<p class="eyebrow">You’re officially on the team</p><h1>Welcome, ${esc((p.display_name||'there').split(' ')[0])}.</h1><p class="gc-welcome-copy">${esc(w.welcomeMessage||'Welcome to GotCracked. This short setup gets your identity, security, and day-one workflow ready.')}</p><div class="gc-welcome-callout"><strong>${esc(w.jobTitle||roleLabel(p.role))}</strong>${w.firstDay?`<br><small>First day · ${esc(w.firstDay)}</small>`:''}${w.managerName?`<br><small>Welcome package from ${esc(w.managerName)}</small>`:''}</div><p class="gc-welcome-copy">Discord authenticates you as a person. Your personal PIN is only used to identify you on shared shop workstations.</p><p class="gc-welcome-message" data-welcome-message></p><div class="gc-welcome-actions"><button class="primary-button" type="button" data-welcome-next="welcome">Start my setup</button></div>`;
    else if(step===1)main.innerHTML=`<p class="eyebrow">Step 2 of 4</p><h1>Make the profile yours.</h1><p class="gc-welcome-copy">Confirm how your name and job title should appear throughout Portal.</p><form data-welcome-profile><div class="gc-welcome-grid"><label>Display name<input name="display_name" maxlength="120" required value="${esc(p.display_name||'')}"></label><label>Job title<input name="job_title" maxlength="120" value="${esc(p.job_title||w.jobTitle||'')}"></label></div><label>Phone (optional)<input name="phone" inputmode="tel" maxlength="40" value="${esc(p.phone||'')}"></label><label>About you (optional)<textarea name="bio" maxlength="600" rows="3">${esc(p.bio||'')}</textarea></label><p class="gc-welcome-message" data-welcome-message></p><div class="gc-welcome-actions"><button class="primary-button" type="submit">Save and continue</button></div></form>`;
    else if(step===2)main.innerHTML=`<p class="eyebrow">Step 3 of 4</p><h1>Secure your shared-workstation identity.</h1><p class="gc-welcome-copy">Your private 4–6 digit PIN tells the trusted Front Desk computer which employee is actually using it.</p><div class="gc-welcome-callout"><strong>Discord</strong> · ${p.discord_linked?'Verified':'Needs attention'}<br><strong>Workstation PIN</strong> · ${pkg.pin_ready?'Ready':'Create one now'}</div>${pkg.pin_ready?'<p class="gc-welcome-message" data-welcome-message></p><div class="gc-welcome-actions"><button class="primary-button" type="button" data-welcome-next="security">Continue</button></div>':`<form data-welcome-pin><div class="gc-welcome-grid"><label>Create PIN<input name="pin" type="password" inputmode="numeric" minlength="4" maxlength="6" pattern="[0-9]{4,6}" required autocomplete="off"></label><label>Confirm PIN<input name="confirm" type="password" inputmode="numeric" minlength="4" maxlength="6" pattern="[0-9]{4,6}" required autocomplete="off"></label></div><p class="gc-welcome-message" data-welcome-message></p><div class="gc-welcome-actions"><button class="primary-button" type="submit">Save PIN and continue</button></div></form>`}`;
    else if(step===3)main.innerHTML=`<p class="eyebrow">Step 4 of 4</p><h1>Your day-one operating rules.</h1><p class="gc-welcome-copy">These habits keep customer data, repairs, and shared computers clean and accountable.</p><div class="gc-welcome-checks"><label><input type="checkbox" data-workflow-check><span><strong>Protect customer information.</strong><br><small>Only access records needed for the repair or customer interaction you are handling.</small></span></label><label><input type="checkbox" data-workflow-check><span><strong>Use your own identity.</strong><br><small>Never share Discord access or your workstation PIN. Switch operators when handing off the Front Desk computer.</small></span></label><label><input type="checkbox" data-workflow-check><span><strong>Leave a clear trail.</strong><br><small>Keep repair stages, notes, payment references, and customer updates factual and current.</small></span></label></div><p class="gc-welcome-message" data-welcome-message></p><div class="gc-welcome-actions"><button class="primary-button" type="button" data-workflow-finish>Finish onboarding</button></div>`;
    else main.innerHTML='<div class="gc-welcome-complete"><div class="gc-ready-icon">✓</div><p class="eyebrow">Setup complete</p><h1>You’re ready for your first day.</h1><p class="gc-welcome-copy">Your Discord identity, staff profile, workstation PIN, and Portal expectations are ready. Welcome to GotCracked.</p><p class="gc-welcome-message" data-welcome-message></p><div class="gc-welcome-actions" style="justify-content:center"><button class="primary-button" type="button" data-onboarding-complete>Enter the Portal</button></div></div>';
    if(!d.open)d.showModal();
  }
  async function reloadPackage(){pkg=await rpc('get_my_onboarding_package');if(pkg?.required)renderWizard();return pkg;}
  async function ack(step){await rpc('acknowledge_my_onboarding_step',{step});await reloadPackage();}

  document.addEventListener('click',async event=>{
    const b=event.target.closest('button');if(!b)return;
    if(b.dataset.welcomeNext){b.disabled=true;try{await ack(b.dataset.welcomeNext);}catch(e){message(e.message,true);b.disabled=false;}}
    if(b.matches('[data-workflow-finish]')){const checks=[...dialog().querySelectorAll('[data-workflow-check]')];if(checks.some(c=>!c.checked)){message('Confirm all three day-one expectations to continue.',true);return;}b.disabled=true;try{await ack('workflow');}catch(e){message(e.message,true);b.disabled=false;}}
    if(b.matches('[data-onboarding-complete]')){b.disabled=true;try{await rpc('complete_my_onboarding');pkg.required=false;dialog().close();location.reload();}catch(e){message(e.message,true);b.disabled=false;}}
    if(b.matches('[data-cancel-staff-invite]')){b.disabled=true;try{await rpc('cancel_staff_invitation',{invitation_id:b.dataset.cancelStaffInvite});await loadOverview();}catch(e){window.GotCrackedDiagnostics?.error?.(e,{context:'Unable to cancel staff invitation'});b.disabled=false;}}
  });
  document.addEventListener('submit',async event=>{
    if(event.target.matches('[data-welcome-profile]')){event.preventDefault();const form=event.target,b=form.querySelector('[type="submit"]');b.disabled=true;try{const fd=new FormData(form),saved=await rpc('update_my_staff_profile',{new_display_name:String(fd.get('display_name')||'').trim(),new_job_title:String(fd.get('job_title')||'').trim()||null,new_phone:String(fd.get('phone')||'').trim()||null,new_bio:String(fd.get('bio')||'').trim()||null,new_avatar_url:profile()?.avatar_url||null});pkg.profile={...pkg.profile,...saved};await ack('profile');}catch(e){message(e.message,true);b.disabled=false;}}
    if(event.target.matches('[data-welcome-pin]')){event.preventDefault();const form=event.target,b=form.querySelector('[type="submit"]'),fd=new FormData(form),pin=String(fd.get('pin')||''),confirm=String(fd.get('confirm')||'');if(!/^\d{4,6}$/.test(pin)){message('Use a 4–6 digit PIN.',true);return;}if(pin!==confirm){message('The PINs do not match.',true);return;}b.disabled=true;try{await rpc('set_my_operator_pin',{pin});pkg.pin_ready=true;await ack('security');}catch(e){message(e.message,true);b.disabled=false;}}
  });
  document.addEventListener('gc-staff-invite-created',()=>setTimeout(loadOverview,120));
  document.addEventListener('gc-view-changed',event=>{const v=typeof event.detail==='string'?event.detail:location.hash.slice(1);if(v==='staff')setTimeout(()=>{upgradeManagementForm();loadOverview();},40);});

  async function init(){installStyle();upgradeManagementForm();const p=profile();if(!p)return;if(p.account_type!=='shared_workstation'){try{await reloadPackage();}catch(e){console.warn('Premium onboarding unavailable:',e?.message);}if(['owner','manager'].includes(p.role))await loadOverview();}}
  init();
  window.GotCrackedPremiumOnboarding={version:'2.0.0',reload:reloadPackage,overview:loadOverview};
})();
